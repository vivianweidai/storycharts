{
  "name": "storycharts",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev www --d1=DB",
    "deploy": "wrangler pages deploy www",
    "db:init": "wrangler d1 execute storycharts --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
